[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    integration: tests qui utilisent une vraie base Chroma ou des fichiers réels
    ollama: tests qui nécessitent Ollama local
    slow: tests lents ou dépendants de modèles
filterwarnings =
    ignore::DeprecationWarning